const manolo : string = "";
export  { manolo };
/* <thead>
      
<!-- loop through each value of the fields to get the table header -->
<th  v-for="(value , property) in todoDTO " :key='value.id' > 
  {{property}}
</th>
<th  v-for="item in todoDTO" :key='item.id' > 
  {{item.name}} 
</th>
 <th  v-for="item in todoDTO" :key='item.id' > 
  {{item.completed}} 
</th>
  
</thead>
<tbody>
<!-- Loop through the list get the each student data -->
<tr v-for="item in todoDTO" :key='item.id' >
<td >{{item.id}}</td>
<td >{{item.name}}</td>
<td >{{item.completed}}</td>
</tr>
</tbody>
 */
/* props: {},
methods: {},
computed: {},
watch:{}
 */